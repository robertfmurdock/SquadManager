plugins {
    id 'com.github.blindpirate.gogradle' version '0.6.5'
}

golang {
    packagePath = 'github.com/robertfmurdock/SquadManager'
}
dependencies {
    golang {
    }
}
dependencies {
    golang {
        test name:'github.com/stretchr/testify'
    }
}